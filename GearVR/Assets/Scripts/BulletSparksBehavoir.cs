using UnityEngine;
using System.Collections;

public class BulletSparksBehavoir : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //Destroy(this.transform.gameobject, 5f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}

}
